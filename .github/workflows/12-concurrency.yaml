name: main-workflow
on: [push]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }} # main-workflow-main 
  cancel-in-progress: true  # if true, present execution will be cancelled, new trigger will happen

jobs: 
  # job name
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the world
        run: sleep 60    